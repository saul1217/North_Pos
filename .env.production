# URL del backend en Railway. Vite la usa en los builds de producción
# (npm run build / npm run dist), horneándola en el .exe.
# npm run dev sigue usando el default (http://localhost:3000).
VITE_API_URL=https://northbikeback-production.up.railway.app
